import type{SpecificationBundle}from"./loader.js";import type{ValidationIssue}from"./validator.js";
export function coverage(bundle:SpecificationBundle,issues:ValidationIssue[]){const counts=(values:string[])=>Object.fromEntries([...new Set(values)].sort().map(v=>[v,values.filter(x=>x===v).length]));const requirements=counts(bundle.requirements.map(r=>r.implementation_status)),dimensions={source:{total:bundle.sources.length,present:bundle.sources.filter(s=>s.file_present).length},requirements:{total:bundle.requirements.length,verified:requirements.verified??0},immutable_rules:{total:bundle.immutableRules.length,tested:bundle.immutableRules.filter(r=>r.test_status==="tested").length},screens:{total:bundle.screens.length,verified:bundle.screens.filter(s=>s.implementation_status==="verified").length},traceability:{total:bundle.requirements.length,mapped:bundle.traceability.length},tests:{mapped_requirements:new Set(bundle.traceability.flatMap(t=>t.test_files.length?[t.requirement_id]:[])).size},issues:counts(issues.map(i=>i.severity))};return{schemaVersion:"1.0.0",generatedAt:new Date().toISOString(),dimensions,requirementStatuses:requirements,issues};}
export function markdown(report:ReturnType<typeof coverage>){return`# Specification Coverage Report\n\nGenerated: ${report.generatedAt}\n\n## Executive summary\n\nBackend domain, database, heartbeat, finance, payments, operations, and reporting have substantial evidence. Coverage is partial because complete authoritative volumes, Appendix A, the public website, full page specifications, systematic explanations, authorization tests, accessibility validation, end-to-end tests, and real PostgreSQL execution evidence are missing.\n\n## Coverage dimensions\n\n| Dimension | Covered | Total |\n|---|---:|---:|\n| Sources present | ${report.dimensions.source.present} | ${report.dimensions.source.total} |\n| Verified requirements | ${report.dimensions.requirements.verified} | ${report.dimensions.requirements.total} |\n| Immutable rules tested | ${report.dimensions.immutable_rules.tested} | ${report.dimensions.immutable_rules.total} |\n| Verified screens | ${report.dimensions.screens.verified} | ${report.dimensions.screens.total} |\n| Traceability mappings | ${report.dimensions.traceability.mapped} | ${report.dimensions.traceability.total} |\n\n## Critical gaps\n\n${report.issues.filter(i=>i.severity==="critical").map(i=>`- **${i.issue_code}** ${i.resource_id}: ${i.message}`).join("\n")||"- None."}\n\n## Source coverage\n\nMissing sources are never treated as extracted or implemented. See source-registry.yaml.\n\n## Immutable-rule coverage\n\nUntested current immutable rules block strict coverage.\n\n## Feature, journey, screen, and explanation coverage\n\nThe public website is missing; several authenticated pages are generic rather than page-complete; user-explanation and recovery coverage remain partial.\n\n## API, data, permission, event, notification, and test coverage\n\nSeed evidence is present, but full automated extraction and exhaustive mapping remain future registry work. Real PostgreSQL tests were unavailable in the latest local environment.\n\n## Prompt coverage\n\nPrompts 001–010 have repository implementation summaries. Original prompt source files are not stored in the repository.\n\n## Open conflicts and deferred work\n\nSee conflicts.yaml and deferred-work.yaml. Appendix A import is a critical blocker.\n\n## Recommended next prompts\n\nPrompt 012 should establish the design system and application shell. Prompt 013 should implement the public Nation Reserve website. Both must begin with prompt-header-template.md and run specification validation first.\n`;}
