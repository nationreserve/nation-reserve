import{describe,expect,it}from"vitest";import{evaluateFeatureFlag,isMaintenanceActive,validateConfigurationValue}from"./domain.js";
describe("operations domain",()=>{it("uses safe false defaults",()=>expect(evaluateFeatureFlag(undefined,{environment:"test",userId:"u"})).toBe(false));it("keeps rollout deterministic",()=>{const flag={key:"v2",enabled:true,status:"active",rolloutPercentage:50,organizationScope:[],userScope:[],environment:"test",version:1};expect(evaluateFeatureFlag(flag,{environment:"test",userId:"same"})).toBe(evaluateFeatureFlag(flag,{environment:"test",userId:"same"}));});it("validates centralized configuration",()=>{expect(validateConfigurationValue("payments.collection_enabled",true)).toBe(true);expect(()=>validateConfigurationValue("unknown",true)).toThrow();});it("honors bounded maintenance windows",()=>expect(isMaintenanceActive({enabled:true,startsAt:new Date(Date.now()-1000),endsAt:new Date(Date.now()+1000)})).toBe(true));});