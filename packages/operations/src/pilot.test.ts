import{describe,expect,it}from"vitest";import{assertPilotGeography,pilotCapacity,pilotConfigurationSchema}from"./pilot.js";
const config=pilotConfigurationSchema.parse({maxPilotOrganizations:10,maxManufacturers:2,maxRobotOwners:5,maxRobots:20,maxContracts:10,maxMonthlyFinancialVolumeMinorUnits:100000,allowedCountryCodes:["US"],allowedRegionCodes:["US-NC"]});
describe("pilot controls",()=>{it("blocks capacity at rather than after the configured limit",()=>{const result=pilotCapacity(config,{organizations:1,manufacturers:2,robotOwners:1,robots:1,contracts:1,monthlyFinancialVolumeMinorUnits:1});expect(result.allowed).toBe(false);expect(result.checks.manufacturers).toBe(false)});it("enforces configured geography",()=>{expect(()=>assertPilotGeography(config,"CA","CA-ON")).toThrow("PILOT_COUNTRY_NOT_ALLOWED");expect(()=>assertPilotGeography(config,"US","US-NY")).toThrow("PILOT_REGION_NOT_ALLOWED");expect(()=>assertPilotGeography(config,"US","US-NC")).not.toThrow()});});
