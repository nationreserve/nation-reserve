import{readFile}from"node:fs/promises";import{resolve}from"node:path";import{describe,expect,it}from"vitest";
describe("Prompt 011 migration",()=>{it("creates rebuildable specification and validation projections",async()=>{const sql=await readFile(resolve("migrations/0013_specification_traceability.sql"),"utf8");for(const table of["specification_sources","specification_requirements","specification_traceability","specification_screens","specification_validation_runs","specification_validation_issues","specification_coverage_waivers"])expect(sql).toContain(`CREATE TABLE ${table}`);expect(sql).toContain("review_at date NOT NULL");});});
