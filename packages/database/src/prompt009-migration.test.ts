import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync(new URL("../migrations/0010_platform_operations_control_plane.sql",import.meta.url),"utf8");describe("Prompt 009 migration",()=>{it("creates versioned flags and configuration",()=>{expect(sql).toContain("feature_flag_versions");expect(sql).toContain("platform_configuration_versions");});it("creates operational jobs and incidents",()=>{expect(sql).toContain("background_job_runs");expect(sql).toContain("worker_heartbeats");expect(sql).toContain("operational_incidents");});it("repairs platform role assignments and adds step-up grants",()=>{expect(sql).toContain("VIEW platform_role_assignments");expect(sql).toContain("administrative_step_up_grants");});});