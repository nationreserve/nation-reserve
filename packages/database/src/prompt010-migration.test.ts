import{readFile}from"node:fs/promises";import{resolve}from"node:path";import{describe,expect,it}from"vitest";
describe("Prompt 010 migration",()=>{it("defines report persistence, warehouse runs, and all materialized projections",async()=>{const sql=await readFile(resolve("migrations/0012_reporting_analytics_warehouse.sql"),"utf8");for(const table of["report_definitions","saved_reports","scheduled_reports","report_runs","report_exports","warehouse_refresh_runs"])expect(sql).toContain(`CREATE TABLE ${table}`);for(const view of["robot_daily_summary","company_daily_summary","owner_daily_summary","financial_daily_summary","payment_daily_summary","heartbeat_daily_summary"])expect(sql).toContain(`CREATE MATERIALIZED VIEW ${view}`);expect(sql).toContain("WITH NO DATA");expect(sql).toContain("reporting-projection-refresh");});});
