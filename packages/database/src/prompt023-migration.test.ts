import{readFile}from"node:fs/promises";import{resolve}from"node:path";import{describe,expect,it}from"vitest";
describe("Prompt 023 migration",()=>{it("defines versioned legal, pilot, readiness, and security controls",async()=>{const sql=await readFile(resolve("migrations/0027_pilot_security_legal_readiness.sql"),"utf8");for(const table of["legal_document_versions","legal_acceptance_records","pilot_configurations","pilot_configuration_versions","pilot_organization_enrollments","readiness_reviews","security_review_findings"])expect(sql).toContain(`CREATE TABLE ${table}`);expect(sql).toContain("legal_acceptance_append_only");expect(sql).toContain("pilot_configuration_versions_append_only");expect(sql).toContain("max_monthly_financial_volume_minor_units")})});
