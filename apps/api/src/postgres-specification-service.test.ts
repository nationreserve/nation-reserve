import{describe,expect,it,vi}from"vitest";import{PostgresSpecificationService}from"./postgres-specification-service.js";
describe("specification authorization",()=>{it("denies ordinary organization users",async()=>{const pool={query:vi.fn().mockResolvedValue({rows:[]})};const service=new PostgresSpecificationService(pool as never,false);await expect(service.overview("11111111-1111-1111-1111-111111111111")).rejects.toMatchObject({message:"PERMISSION_DENIED",statusCode:403});});it("allows configured platform operations read access",async()=>{const query=vi.fn().mockImplementation((sql:string)=>Promise.resolve(sql.includes("platform_role_assignments")?{rows:[{role:"operations"}]}:{rows:[]})),service=new PostgresSpecificationService({query}as never,false),result=await service.collection("11111111-1111-1111-1111-111111111111","requirements");expect(result.items.length).toBeGreaterThan(20);});it("keeps production synchronization disabled by default",async()=>{const query=vi.fn().mockResolvedValue({rows:[{role:"platform_admin"}]}),service=new PostgresSpecificationService({query}as never,false);await expect(service.sync("11111111-1111-1111-1111-111111111111")).rejects.toMatchObject({message:"SPECIFICATION_SYNC_DISABLED"});});});
