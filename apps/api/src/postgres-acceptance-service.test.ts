import{describe,expect,it,vi}from"vitest";import{PostgresAcceptanceService}from"./postgres-acceptance-service.js";
describe("acceptance waiver step-up",()=>{it("rejects a caller token without a matching session-bound grant",async()=>{const query=vi.fn().mockResolvedValueOnce({rowCount:1,rows:[{}]}).mockResolvedValueOnce({rowCount:0,rows:[]});const service=new PostgresAcceptanceService({query} as never);await expect(service.createWaiver("00000000-0000-4000-8000-000000000001","00000000-0000-4000-8000-000000000002","caller-controlled-token",{gapId:"GAP-1",reason:"A sufficiently detailed reason",temporaryBehavior:"A sufficiently detailed behavior",risk:"A sufficiently detailed risk",expiresAt:"2026-09-01T00:00:00.000Z",followUpIssue:"SEC-1",affectedOrganizations:[]})).rejects.toMatchObject({code:"STEP_UP_INVALID"});expect(String(query.mock.calls[1]?.[0])).toContain("used_at IS NULL");expect(String(query.mock.calls[1]?.[0])).toContain("purpose='acceptance_waiver'")})});
