// @vitest-environment jsdom
import{afterEach,describe,expect,it,vi}from"vitest";import{cleanup,render,screen}from"@testing-library/react";import{ManufacturerPage}from"./ManufacturerPages.js";vi.mock("./auth-client.js",()=>({api:{get:vi.fn().mockResolvedValue({items:[]}),post:vi.fn()}}));afterEach(()=>cleanup());describe("Prompt 017 responsive coverage",()=>{for(const width of[360,390,768,1024,1440])it(`keeps discovery usable at ${width}px`,()=>{Object.defineProperty(window,"innerWidth",{configurable:true,value:width});render(<ManufacturerPage path="/manufacturer/work-orders"/>);expect(screen.getByLabelText("Service region")).toBeVisible();expect(screen.getByLabelText("Distance radius")).toBeVisible()})});
