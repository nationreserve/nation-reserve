import{render,screen}from"@testing-library/react";import{afterEach,describe,expect,it,vi}from"vitest";import{SpecificationPage}from"./SpecificationPages.js";
afterEach(()=>vi.unstubAllGlobals());describe("Specification dashboard",()=>{it("renders separate coverage dimensions and prominent critical gaps",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,status:200,json:async()=>({dimensions:{source:{total:31,present:24},requirements:{total:37,verified:18},screens:{total:6,verified:1}},issues:[{severity:"critical",issue_code:"SPEC_SOURCE_MISSING",resource_id:"APP-A",message:"Source missing",suggested_resolution:"Import source"}]})}));render(<SpecificationPage path="/platform/specification/overview"/>);expect(await screen.findByText("Specification coverage overview")).toBeInTheDocument();expect(screen.getByText("source")).toBeInTheDocument();expect(screen.getByText("SPEC_SOURCE_MISSING")).toBeInTheDocument();expect(screen.queryByText(/overall score/i)).not.toBeInTheDocument();});});
