import{afterEach,describe,expect,it,vi}from'vitest';import{cleanup,render,screen}from'@testing-library/react';import{OrganizationProvider,developmentSessions}from'@nation-reserve/application-shell';import{QueuePage,SupportPage}from'./PlatformCompletionPages.js';import{api}from'./auth-client.js';vi.mock('./auth-client.js',()=>({api:{get:vi.fn(),post:vi.fn()}}));const get=vi.mocked(api.get);afterEach(()=>{cleanup();vi.clearAllMocks()});
describe('universal support and queue UI',()=>{
 it('gives an authenticated Company an obvious complete support form',async()=>{get.mockResolvedValue({items:[]});render(<OrganizationProvider initial={developmentSessions.company!}><SupportPage path="/support"/></OrganizationProvider>);expect(screen.getByRole('heading',{name:'Contact Support'})).toBeInTheDocument();expect(screen.getByLabelText('Category')).toBeInTheDocument();expect(screen.getByLabelText(/File or screenshot/)).toBeInTheDocument();expect(screen.getByLabelText(/Include diagnostic information/)).toBeInTheDocument();expect(await screen.findByText('No support requests yet.')).toBeInTheDocument()});
 it('highlights a Robot Owner position but anonymizes other entries',async()=>{get.mockResolvedValue({privacy:'Other participants are anonymized.',currentPosition:4,items:[{id:'a',position:3,participant:'Participant 000003',status:'available',availableCents:1000,reservedCents:0,appliedCents:0},{id:'b',position:4,participant:'Owner Example',isMine:true,status:'available',availableCents:2000,reservedCents:0,appliedCents:0}]});render(<OrganizationProvider initial={developmentSessions.owner!}><QueuePage/></OrganizationProvider>);expect(await screen.findByText('Your current queue position: #4')).toBeInTheDocument();expect(screen.getByText('Participant 000003')).toBeInTheDocument();expect(screen.queryByText(/@/)).not.toBeInTheDocument()});
});
