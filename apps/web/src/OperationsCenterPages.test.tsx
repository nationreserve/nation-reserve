import{render,screen}from"@testing-library/react";import{afterEach,describe,expect,it,vi}from"vitest";import{OperationsCenterPage}from"./OperationsCenterPages.js";afterEach(()=>vi.unstubAllGlobals());describe("Operations Center",()=>{it("renders platform health domains without mutation controls",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,status:200,json:async()=>({organizations:{total:4},robots:{registered:2},contracts:{active:1},heartbeats:{per_minute:10},financial:{open_invoices:1},payments:{failed_collections:0},workers:{running:1},infrastructure:{database:"roboworkpool"}})}));render(<OperationsCenterPage path="/platform"/>);expect(await screen.findByText("Platform operations")).toBeInTheDocument();expect(screen.getByText("organizations")).toBeInTheDocument();expect(screen.getByText(/cannot alter immutable financial history/)).toBeInTheDocument();});it("warns that maintenance requires step-up",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,status:200,json:async()=>({items:[]})}));render(<OperationsCenterPage path="/platform/maintenance"/>);expect(await screen.findByText("Step-up authentication required")).toBeInTheDocument();expect(screen.getByText(/Critical workers continue running/)).toBeInTheDocument();});});