import{render,screen}from"@testing-library/react";import{afterEach,describe,expect,it,vi}from"vitest";import{PaymentPage}from"./PaymentPages.js";
afterEach(()=>vi.unstubAllGlobals());describe("payment operations UI",()=>{
 it("never labels a submitted collection as settled",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,status:200,json:async()=>({items:[{id:"1",attempt_number:"PAY-1",status:"submitted",amount_minor_units:500}]})}));render(<PaymentPage path="/platform/payments"/>);expect(await screen.findByText("Submitted — awaiting processor confirmation")).toBeInTheDocument();expect(screen.queryByText(/^Settled$/)).not.toBeInTheDocument();});
 it("distinguishes financial readiness from processor payout readiness",async()=>{sessionStorage.setItem("currentOrganizationId",crypto.randomUUID());vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,status:200,json:async()=>({items:[{id:"1",provider:"fake",status:"pending_verification"}]})}));render(<PaymentPage path="/owner/settings/payout-account"/>);expect(await screen.findByText("Processor requirements pending")).toBeInTheDocument();expect(screen.getByText(/Financial readiness alone does not mean payout ready/)).toBeInTheDocument();});
});