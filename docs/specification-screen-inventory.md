# Specification Screen Inventory

A screen record requires persona, purpose, explanation, actions, displayed data, all UI states, responsive behavior, accessibility, API dependencies, permissions, prompt, requirements, and tests. A route and title alone are insufficient.

Prompt 012 should use `screens.yaml` to establish the design system and application shell, then complete authenticated page specifications. Prompt 013 should implement the public inventory.
