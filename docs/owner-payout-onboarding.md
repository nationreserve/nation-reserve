# Owner payout onboarding

RoboWorkPool creates a provider connected account and sends the owner to hosted onboarding. The processor—not RoboWorkPool—collects identity and bank details. Refresh retrieves capability state and projects `onboarding`, `pending_verification`, or `active`. UI language must say “Processor verification complete” or “Processor requirements pending.”