# Specification Conflicts

Conflicts record both sources, affected requirements, severity, precedence analysis, recommended resolution, and approval state. Canonical sources are changed through repository review. The production dashboard may expose review state but does not casually rewrite specification files.

Critical open conflicts block strict coverage. Accepted exceptions require authorization, audit evidence, and a review date.
