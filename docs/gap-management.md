# Gap Management

Gaps use the six Prompt 022 classifications and statuses `open`, `in_progress`, `resolved`, or `waived`. Resolution requires linked evidence and a rerun. No one may delete historical gap, run, event or waiver records to improve a score. Critical failures create or update a gap and keep the release blocked.
