# Training-file uploads

Large private uploads use organization-scoped object keys, short-lived signed multipart URLs, resume, integrity verification, malware scanning, quarantine, processing jobs, metadata, versions, retention, deletion, legal hold, and audit. Files are not proxied through the primary API.
