# Status system

The centralized `statuses` registry assigns each key a display label, explanation, tone, icon, entity scope, next action, and terminal state. `StatusBadge` always emits text; `StatusExplanation` provides meaning and next steps. Submitted, processing, settled, financially ready, ready for payout, and paid are never interchangeable.
