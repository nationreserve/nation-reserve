schemaVersion: "1.0.0"
items:
  - {workflow_id: WF-ROBOT-LIFECYCLE, name: "Robot lifecycle", domain: robot, entity: robot, states: [registered, ownership_verified, activated, available, assigned, operating, offline, maintenance, retired], transitions: [register, claim, activate, make_available, assign, start_operating, mark_offline, maintain, retire], transition_guards: ["Eligibility and authorization required"], authorized_roles: [manufacturer_administrator, robot_owner_owner, platform_operations], side_effects: [state_history], audit_actions: [robot.state.changed], outbox_events: [robot.state.changed], notifications: [], failure_states: [activation_failed, ownership_disputed], recovery_actions: [retry_activation, resolve_claim, maintenance], related_requirements: [RWP-ROBOT-0001], test_status: partial}
  - {workflow_id: WF-CONTRACT, name: "Contract and assignment fulfillment", domain: contract, entity: contract, states: [draft, pending_approval, accepted, active, completed, cancelled], transitions: [submit, approve, allocate, activate, complete, cancel], transition_guards: ["Manufacturer fulfillment and compatible eligible robot"], authorized_roles: [hiring_company_administrator, manufacturer_administrator], side_effects: [assignment_creation], audit_actions: [contract.approved], outbox_events: [contract.approved], notifications: [], failure_states: [partially_staffed, allocation_failed], recovery_actions: [allocate_remaining, replace_robot], related_requirements: [RWP-CONTRACT-0001, RWP-ASSIGN-0001], test_status: partial}
  - {workflow_id: WF-VERIFIED-TIME, name: "Heartbeat to verified time", domain: heartbeat, entity: verified_operating_interval, states: [open, closed, held, finalized, invalidated], transitions: [accept_heartbeat, close, hold, finalize, invalidate], transition_guards: ["Valid signed heartbeat and eligible assignment"], authorized_roles: [system, platform_operations], side_effects: [operating_interval, accrual_candidate], audit_actions: [], outbox_events: [verified_time.finalized], notifications: [], failure_states: [rejected_heartbeat, held], recovery_actions: [next_valid_heartbeat, review_hold], related_requirements: [RWP-HEARTBEAT-0001, RWP-TIME-0001], test_status: tested}
  - {workflow_id: WF-PAYMENT, name: "Company payment", domain: payment, entity: payment_attempt, states: [created, submitted, processing, succeeded, settled, failed, refunded, disputed], transitions: [submit, process, succeed, settle, fail, refund, dispute], transition_guards: ["Verified provider evidence and idempotency"], authorized_roles: [platform_billing, system], side_effects: [processor_event, clearing_entry], audit_actions: [], outbox_events: [payment.updated], notifications: [payment.updated], failure_states: [failed, unknown, disputed], recovery_actions: [retry, reconcile, investigate], related_requirements: [RWP-PAYMENT-0002, RWP-PAYMENT-0003, RWP-BILLING-0001], test_status: partial}
  - {workflow_id: WF-SCHEDULED-REPORT, name: "Scheduled report", domain: reporting, entity: scheduled_report, states: [enabled, claimed, generated, delivered, failed, disabled], transitions: [schedule, claim, generate, deliver, fail, disable], transition_guards: ["Owner retains report permission"], authorized_roles: [report_creator, system], side_effects: [report_run, export], audit_actions: [], outbox_events: [report.scheduled.ready], notifications: [report.scheduled.ready], failure_states: [failed], recovery_actions: [retry_next_schedule, operator_review], related_requirements: [RWP-REPORT-0001], test_status: partial}
