# Robot management UI

Robot detail consumes the existing permission-checked robot endpoint and per-robot operating-time endpoint. Inventory filters are present, but rows remain unavailable until an owner-scoped inventory API exists. Availability cannot be changed until a policy-enforcing mutation is implemented.
