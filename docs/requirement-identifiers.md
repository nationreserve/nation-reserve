# Requirement Identifiers

Identifiers use `RWP-[DOMAIN]-[NUMBER]`, such as `RWP-HEARTBEAT-0001`. IDs are permanent after publication. A superseded requirement keeps its ID and history; its number is never reassigned. New domains should use a short stable uppercase name and the next unused four-digit sequence.
