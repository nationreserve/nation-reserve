# Manufacturer heartbeat integration

Sandbox and production use separate credentials and validation. Signed heartbeat rules cover timestamps, replay, sequence, serial and assignment mapping, retry, clock drift, rate limits, diagnostics, and versioning.
