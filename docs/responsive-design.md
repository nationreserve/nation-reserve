# Responsive design

Tokens define 360, 390, 768, 1024, and 1440px representative widths. The shell switches from sidebar to drawer below desktop. Headers stack, forms use one column, long text wraps, financial values use tabular numerals, and tables retain an overflow container until a feature screen supplies its approved card transformation. Validate at 200% zoom and with long organization names.
