# Ownership workflow

An owner submits a registered robot platform ID and manufacturer transfer code. The existing claim API creates the claim for verification. Submission is not approval, activation, assignment, or earnings eligibility. Owner-scoped claim list and detail APIs remain deferred.
