// Edge-only type shim. The API service imports these symbols with `import type`,
// so this file carries no runtime payment or financial implementation.
export type PaymentConfig=any;
export type PaymentProvider=any;
export type VerifiedWebhookEvent=any;
