const env=process.env.DEPLOY_ENVIRONMENT||process.env.NODE_ENV||"development",production=env==="production",required=["DATABASE_URL","REDIS_URL","S3_BUCKET","WEB_ORIGIN"],missing=required.filter(key=>!process.env[key]);if(missing.length)throw new Error(`Missing runtime configuration: ${missing.join(", ")}`);
const forbiddenProduction=[["PAYMENT_PROVIDER","fake"],["PAYMENT_PROVIDER_ENVIRONMENT","test"],["PAYMENT_EXECUTION_ENABLED","false"],["COOKIE_SECURE","false"]];if(production){for(const[key,value]of forbiddenProduction)if((process.env[key]||"").toLowerCase()===value)throw new Error(`${key}=${value} is forbidden in production`);for(const key of ["DATABASE_URL","REDIS_URL","S3_ENDPOINT","WEB_ORIGIN"])if((process.env[key]||"").includes("localhost"))throw new Error(`${key} cannot reference localhost in production`);if(!process.env.WEB_ORIGIN?.startsWith("https://"))throw new Error("Production WEB_ORIGIN must use HTTPS");}
for(const key of Object.keys(process.env))if(/SECRET|TOKEN|PASSWORD|PRIVATE_KEY/.test(key)&&process.env[key]?.includes("change-me"))throw new Error(`${key} contains a development placeholder`);console.log(`Environment ${env} passed isolation and adapter checks.`);
