const base=process.env.SMOKE_BASE_URL;if(!base)throw new Error("SMOKE_BASE_URL is required");for(const path of ["/live","/health","/ready"]){const response=await fetch(new URL(path,base),{signal:AbortSignal.timeout(10000)});if(!response.ok)throw new Error(`${path} failed with ${response.status}`);console.log(`${path}: ${response.status}`);}console.log("Post-deployment smoke checks passed.");
