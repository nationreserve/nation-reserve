console.log("Queue replay validation requires TEST_DATABASE_URL and an isolated worker deployment.");if(!process.env.TEST_DATABASE_URL)throw new Error("TEST_DATABASE_URL is required; production databases are forbidden");if(/prod/i.test(process.env.TEST_DATABASE_URL))throw new Error("Refusing resilience test against a production-like database identifier");console.log("Isolation guard passed; invoke the integration recovery suite in the test environment.");
